projects:
  batty:
    schemaPath: amplify/backend/api/batty/build/schema.graphql
    includes:
      - src/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/types/API.ts
        docsFilePath: src/graphql
extensions:
  amplify:
    version: 3
